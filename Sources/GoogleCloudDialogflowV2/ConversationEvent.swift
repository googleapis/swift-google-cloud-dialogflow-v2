// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if Agents && AnswerRecords && Contexts && ConversationDatasets && ConversationModels && ConversationProfiles && Conversations && Documents && EncryptionSpecService && EntityTypes && Environments && Fulfillments && GeneratorEvaluations && Generators && Intents && KnowledgeBases && Participants && SessionEntityTypes && Sessions && SipTrunks && Tools && Versions
  import Foundation
  import GoogleCloudWkt
  import GoogleRpc

  /// Represents a notification sent to Pub/Sub subscribers for conversation
  /// lifecycle events.
  public struct ConversationEvent: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// The unique identifier of the conversation this notification
    /// refers to.
    /// Format: `projects/<Project ID>/conversations/<Conversation ID>`.
    public var conversation: Swift.String = Swift.String()

    /// The type of the event that this notification refers to.
    public var type: ConversationEvent.Type_ = ConversationEvent.Type_()

    /// More detailed information about an error. Only set for type
    /// UNRECOVERABLE_ERROR_IN_PHONE_CALL.
    public var errorStatus: GoogleRpc.Status? = nil

    /// Payload of conversation event.
    public var payload: OneOf_Payload? = nil

    /// Initialize a new instance of `ConversationEvent`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ConversationEvent().with { $0.conversation = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case conversation = "conversation"
      case type = "type"
      case errorStatus = "errorStatus"
      case newMessagePayload = "newMessagePayload"
      case newRecognitionResultPayload = "newRecognitionResultPayload"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.conversation = try container.decode(Swift.String.self, forKey: .conversation)
      self.type = try container.decode(ConversationEvent.Type_.self, forKey: .type)
      self.errorStatus = try container.decodeIfPresent(GoogleRpc.Status.self, forKey: .errorStatus)

      var payload: OneOf_Payload? = nil
      let payloadCheckAndSet = {
        if payload != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'payload'"))
        }
        payload = $0
      }
      if let newMessagePayload = try container.decodeIfPresent(
        Message?.self, forKey: .newMessagePayload)
      {
        try payloadCheckAndSet(.newMessagePayload(newMessagePayload))
      }
      if let newRecognitionResultPayload = try container.decodeIfPresent(
        StreamingRecognitionResult?.self, forKey: .newRecognitionResultPayload)
      {
        try payloadCheckAndSet(.newRecognitionResultPayload(newRecognitionResultPayload))
      }
      self.payload = payload
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.conversation, forKey: .conversation)
      try container.encode(self.type, forKey: .type)
      try container.encode(self.errorStatus, forKey: .errorStatus)

      if let choice = self.payload {
        switch choice {
        case .newMessagePayload(let value):
          try container.encode(value, forKey: .newMessagePayload)
        case .newRecognitionResultPayload(let value):
          try container.encode(value, forKey: .newRecognitionResultPayload)
        }
      }
    }

    /// Enumeration of the types of events available.
    public enum Type_: Codable, Equatable, Sendable {
      /// Type not set.
      case unspecified
      /// A new conversation has been opened. This is fired when a telephone call
      /// is answered, or a conversation is created via the API.
      case conversationStarted
      /// An existing conversation has closed. This is fired when a telephone call
      /// is terminated, or a conversation is closed via the API.
      /// The event is fired for every CompleteConversation call, even if the
      /// conversation is already closed.
      case conversationFinished
      /// An existing conversation has received notification from Dialogflow that
      /// human intervention is required.
      case humanInterventionNeeded
      /// Unrecoverable error during a telephone call.
      ///
      /// In general non-recoverable errors only occur if something was
      /// misconfigured in the ConversationProfile corresponding to the call. After
      /// a non-recoverable error, Dialogflow may stop responding.
      ///
      /// We don't fire this event:
      ///
      /// * in an API call because we can directly return the error, or,
      /// * when we can recover from an error.
      case unrecoverableError
      /// An existing conversation has received a new message, either from API or
      /// telephony. It is configured in
      /// [ConversationProfile.new_message_event_notification_config][google.cloud.dialogflow.v2.ConversationProfile.new_message_event_notification_config]
      ///
      /// [google.cloud.dialogflow.v2.ConversationProfile.new_message_event_notification_config]: <doc:ConversationProfile/newMessageEventNotificationConfig>
      case newMessage
      /// An existing conversation has received a new speech recognition result.
      /// This is mainly for delivering intermediate transcripts. The notification
      /// is configured in
      /// [ConversationProfile.new_recognition_event_notification_config][].
      case newRecognitionResult
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .conversationStarted: return 1
        case .conversationFinished: return 2
        case .humanInterventionNeeded: return 3
        case .unrecoverableError: return 4
        case .newMessage: return 5
        case .newRecognitionResult: return 7
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "TYPE_UNSPECIFIED"
        case .conversationStarted: return "CONVERSATION_STARTED"
        case .conversationFinished: return "CONVERSATION_FINISHED"
        case .humanInterventionNeeded: return "HUMAN_INTERVENTION_NEEDED"
        case .unrecoverableError: return "UNRECOVERABLE_ERROR"
        case .newMessage: return "NEW_MESSAGE"
        case .newRecognitionResult: return "NEW_RECOGNITION_RESULT"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Type_/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "TYPE_UNSPECIFIED": self = .unspecified
        case "CONVERSATION_STARTED": self = .conversationStarted
        case "CONVERSATION_FINISHED": self = .conversationFinished
        case "HUMAN_INTERVENTION_NEEDED": self = .humanInterventionNeeded
        case "NEW_MESSAGE": self = .newMessage
        case "NEW_RECOGNITION_RESULT": self = .newRecognitionResult
        case "UNRECOVERABLE_ERROR": self = .unrecoverableError
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Type_/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .conversationStarted
        case 2: self = .conversationFinished
        case 3: self = .humanInterventionNeeded
        case 4: self = .unrecoverableError
        case 5: self = .newMessage
        case 7: self = .newRecognitionResult
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .conversationStarted: return try container.encode(1)
        case .conversationFinished: return try container.encode(2)
        case .humanInterventionNeeded: return try container.encode(3)
        case .unrecoverableError: return try container.encode(4)
        case .newMessage: return try container.encode(5)
        case .newRecognitionResult: return try container.encode(7)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Payload of conversation event.
    public enum OneOf_Payload: Codable, Equatable, Sendable {
      /// Payload of NEW_MESSAGE event.
      indirect case newMessagePayload(Message?)
      /// Payload of NEW_RECOGNITION_RESULT event.
      indirect case newRecognitionResultPayload(StreamingRecognitionResult?)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.v2.ConversationEvent"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
