// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if AnswerRecords || Intents || Participants || Sessions
  import Foundation
  import GoogleCloudWkt

  /// An intent categorizes an end-user's intention for one conversation turn. For
  /// each agent, you define many intents, where your combined intents can handle a
  /// complete conversation. When an end-user writes or says something, referred to
  /// as an end-user expression or end-user input, Dialogflow matches the end-user
  /// input to the best intent in your agent. Matching an intent is also known as
  /// intent classification.
  ///
  /// For more information, see the [intent
  /// guide](https://cloud.google.com/dialogflow/docs/intents-overview).
  public struct Intent: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. The unique identifier of this intent.
    /// Required for
    /// [Intents.UpdateIntent][google.cloud.dialogflow.v2.Intents.UpdateIntent] and
    /// [Intents.BatchUpdateIntents][google.cloud.dialogflow.v2.Intents.BatchUpdateIntents]
    /// methods.
    /// Format: `projects/<Project ID>/agent/intents/<Intent ID>`.
    ///
    /// [google.cloud.dialogflow.v2.Intents.BatchUpdateIntents]: <doc:IntentsClient/batchUpdateIntents(request:)>
    /// [google.cloud.dialogflow.v2.Intents.UpdateIntent]: <doc:IntentsClient/updateIntent(request:)>
    public var name: Swift.String = Swift.String()

    /// Required. The name of this intent.
    public var displayName: Swift.String = Swift.String()

    /// Optional. Indicates whether webhooks are enabled for the intent.
    public var webhookState: Intent.WebhookState = Intent.WebhookState()

    /// Optional. The priority of this intent. Higher numbers represent higher
    /// priorities.
    ///
    /// - If the supplied value is unspecified or 0, the service
    ///   translates the value to 500,000, which corresponds to the
    ///   `Normal` priority in the console.
    /// - If the supplied value is negative, the intent is ignored
    ///   in runtime detect intent requests.
    public var priority: Swift.Int32 = Swift.Int32()

    /// Optional. Indicates whether this is a fallback intent.
    public var isFallback: Swift.Bool = Swift.Bool()

    /// Optional. Indicates whether Machine Learning is disabled for the intent.
    /// Note: If `ml_disabled` setting is set to true, then this intent is not
    /// taken into account during inference in `ML ONLY` match mode. Also,
    /// auto-markup in the UI is turned off.
    public var mlDisabled: Swift.Bool = Swift.Bool()

    /// Optional. Indicates that a live agent should be brought in to handle the
    /// interaction with the user. In most cases, when you set this flag to true,
    /// you would also want to set end_interaction to true as well. Default is
    /// false.
    public var liveAgentHandoff: Swift.Bool = Swift.Bool()

    /// Optional. Indicates that this intent ends an interaction. Some integrations
    /// (e.g., Actions on Google or Dialogflow phone gateway) use this information
    /// to close interaction with an end user. Default is false.
    public var endInteraction: Swift.Bool = Swift.Bool()

    /// Optional. The list of context names required for this intent to be
    /// triggered.
    /// Format: `projects/<Project ID>/agent/sessions/-/contexts/<Context ID>`.
    public var inputContextNames: [Swift.String] = []

    /// Optional. The collection of event names that trigger the intent.
    /// If the collection of input contexts is not empty, all of the contexts must
    /// be present in the active user session for an event to trigger this intent.
    /// Event names are limited to 150 characters.
    public var events: [Swift.String] = []

    /// Optional. The collection of examples that the agent is
    /// trained on.
    public var trainingPhrases: [Intent.TrainingPhrase] = []

    /// Optional. The name of the action associated with the intent.
    /// Note: The action name must not contain whitespaces.
    public var action: Swift.String = Swift.String()

    /// Optional. The collection of contexts that are activated when the intent
    /// is matched. Context messages in this collection should not set the
    /// parameters field. Setting the `lifespan_count` to 0 will reset the context
    /// when the intent is matched.
    /// Format: `projects/<Project ID>/agent/sessions/-/contexts/<Context ID>`.
    public var outputContexts: [Context] = []

    /// Optional. Indicates whether to delete all contexts in the current
    /// session when this intent is matched.
    public var resetContexts: Swift.Bool = Swift.Bool()

    /// Optional. The collection of parameters associated with the intent.
    public var parameters: [Intent.Parameter] = []

    /// Optional. The collection of rich messages corresponding to the
    /// `Response` field in the Dialogflow console.
    public var messages: [Intent.Message] = []

    /// Optional. The list of platforms for which the first responses will be
    /// copied from the messages in PLATFORM_UNSPECIFIED (i.e. default platform).
    public var defaultResponsePlatforms: [Intent.Message.Platform] = []

    /// Output only.
    /// Read-only. The unique identifier of the root intent in the chain of
    /// followup intents. It identifies the correct followup intents chain for
    /// this intent. We populate this field only in the output.
    ///
    /// Format: `projects/<Project ID>/agent/intents/<Intent ID>`.
    public var rootFollowupIntentName: Swift.String = Swift.String()

    /// Read-only after creation. The unique identifier of the parent intent in the
    /// chain of followup intents. You can set this field when creating an intent,
    /// for example with
    /// [CreateIntent][google.cloud.dialogflow.v2.Intents.CreateIntent] or
    /// [BatchUpdateIntents][google.cloud.dialogflow.v2.Intents.BatchUpdateIntents],
    /// in order to make this intent a followup intent.
    ///
    /// It identifies the parent followup intent.
    /// Format: `projects/<Project ID>/agent/intents/<Intent ID>`.
    ///
    /// [google.cloud.dialogflow.v2.Intents.BatchUpdateIntents]: <doc:IntentsClient/batchUpdateIntents(request:)>
    /// [google.cloud.dialogflow.v2.Intents.CreateIntent]: <doc:IntentsClient/createIntent(request:)>
    public var parentFollowupIntentName: Swift.String = Swift.String()

    /// Output only. Read-only. Information about all followup intents that have
    /// this intent as a direct or indirect parent. We populate this field only in
    /// the output.
    public var followupIntentInfo: [Intent.FollowupIntentInfo] = []

    /// Initialize a new instance of `Intent`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Intent().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Represents an example that the agent is trained on.
    public struct TrainingPhrase: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Output only. The unique identifier of this training phrase.
      public var name: Swift.String = Swift.String()

      /// Required. The type of the training phrase.
      public var type: Intent.TrainingPhrase.Type_ = Intent.TrainingPhrase.Type_()

      /// Required. The ordered list of training phrase parts.
      /// The parts are concatenated in order to form the training phrase.
      ///
      /// Note: The API does not automatically annotate training phrases like the
      /// Dialogflow Console does.
      ///
      /// Note: Do not forget to include whitespace at part boundaries,
      /// so the training phrase is well formatted when the parts are concatenated.
      ///
      /// If the training phrase does not need to be annotated with parameters,
      /// you just need a single part with only the
      /// [Part.text][google.cloud.dialogflow.v2.Intent.TrainingPhrase.Part.text]
      /// field set.
      ///
      /// If you want to annotate the training phrase, you must create multiple
      /// parts, where the fields of each part are populated in one of two ways:
      ///
      /// -   `Part.text` is set to a part of the phrase that has no parameters.
      /// -   `Part.text` is set to a part of the phrase that you want to annotate,
      ///     and the `entity_type`, `alias`, and `user_defined` fields are all
      ///     set.
      ///
      /// [google.cloud.dialogflow.v2.Intent.TrainingPhrase.Part.text]: <doc:Intent/TrainingPhrase/Part/text>
      public var parts: [Intent.TrainingPhrase.Part] = []

      /// Optional. Indicates how many times this example was added to
      /// the intent. Each time a developer adds an existing sample by editing an
      /// intent or training, this counter is increased.
      public var timesAddedCount: Swift.Int32 = Swift.Int32()

      /// Initialize a new instance of `TrainingPhrase`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = TrainingPhrase().with { $0.name = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Represents a part of a training phrase.
      public struct Part: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. The text for this part.
        public var text: Swift.String = Swift.String()

        /// Optional. The entity type name prefixed with `@`.
        /// This field is required for annotated parts of the training phrase.
        public var entityType: Swift.String = Swift.String()

        /// Optional. The parameter name for the value extracted from the
        /// annotated part of the example.
        /// This field is required for annotated parts of the training phrase.
        public var alias: Swift.String = Swift.String()

        /// Optional. Indicates whether the text was manually annotated.
        /// This field is set to true when the Dialogflow Console is used to
        /// manually annotate the part. When creating an annotated part with the
        /// API, you must set this to true.
        public var userDefined: Swift.Bool = Swift.Bool()

        /// Initialize a new instance of `Part`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Part().with { $0.text = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.TrainingPhrase.Part"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Represents different types of training phrases.
      public enum Type_: Codable, Equatable, Sendable {
        /// Not specified. This value should never be used.
        case unspecified
        /// Examples do not contain @-prefixed entity type names, but example parts
        /// can be annotated with entity types.
        case example
        /// Templates are not annotated with entity types, but they can contain
        /// @-prefixed entity type names as substrings.
        /// Template mode has been deprecated. Example mode is the only supported
        /// way to create new training phrases. If you have existing training
        /// phrases that you've created in template mode, those will continue to
        /// work.
        @available(*, deprecated)
        case template
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .example: return 1
          case .template: return 2
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: Swift.String? {
          switch self {
          case .unspecified: return "TYPE_UNSPECIFIED"
          case .example: return "EXAMPLE"
          case .template: return "TEMPLATE"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Type_/unknownStringValue(_:)).
        public init(stringValue: Swift.String) {
          switch stringValue {
          case "TYPE_UNSPECIFIED": self = .unspecified
          case "EXAMPLE": self = .example
          case "TEMPLATE": self = .template
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Type_/unknownIntValue(_:)).
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .example
          case 2: self = .template
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode(0)
          case .example: return try container.encode(1)
          case .template: return try container.encode(2)
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.TrainingPhrase"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents intent parameters.
    public struct Parameter: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// The unique identifier of this parameter.
      public var name: Swift.String = Swift.String()

      /// Required. The name of the parameter.
      public var displayName: Swift.String = Swift.String()

      /// Optional. The definition of the parameter value. It can be:
      ///
      /// - a constant string,
      /// - a parameter value defined as `$parameter_name`,
      /// - an original parameter value defined as `$parameter_name.original`,
      /// - a parameter value from some context defined as
      ///   `#context_name.parameter_name`.
      public var value: Swift.String = Swift.String()

      /// Optional. The default value to use when the `value` yields an empty
      /// result.
      /// Default values can be extracted from contexts by using the following
      /// syntax: `#context_name.parameter_name`.
      public var defaultValue: Swift.String = Swift.String()

      /// Optional. The name of the entity type, prefixed with `@`, that
      /// describes values of the parameter. If the parameter is
      /// required, this must be provided.
      public var entityTypeDisplayName: Swift.String = Swift.String()

      /// Optional. Indicates whether the parameter is required. That is,
      /// whether the intent cannot be completed without collecting the parameter
      /// value.
      public var mandatory: Swift.Bool = Swift.Bool()

      /// Optional. The collection of prompts that the agent can present to the
      /// user in order to collect a value for the parameter.
      public var prompts: [Swift.String] = []

      /// Optional. Indicates whether the parameter represents a list of values.
      public var isList: Swift.Bool = Swift.Bool()

      /// Initialize a new instance of `Parameter`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Parameter().with { $0.name = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Parameter"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// A rich response message.
    /// Corresponds to the intent `Response` field in the Dialogflow console.
    /// For more information, see
    /// [Rich response
    /// messages](https://cloud.google.com/dialogflow/docs/intents-rich-messages).
    public struct Message: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. The platform that this message is intended for.
      public var platform: Intent.Message.Platform = Intent.Message.Platform()

      /// Required. The rich response message.
      public var message: OneOf_Message? = nil

      /// Initialize a new instance of `Message`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Message().with { $0.text = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case text = "text"
        case image = "image"
        case quickReplies = "quickReplies"
        case card = "card"
        case payload = "payload"
        case simpleResponses = "simpleResponses"
        case basicCard = "basicCard"
        case suggestions = "suggestions"
        case linkOutSuggestion = "linkOutSuggestion"
        case listSelect = "listSelect"
        case carouselSelect = "carouselSelect"
        case browseCarouselCard = "browseCarouselCard"
        case tableCard = "tableCard"
        case mediaContent = "mediaContent"
        case platform = "platform"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.platform = try container.decode(Intent.Message.Platform.self, forKey: .platform)

        var message: OneOf_Message? = nil
        let messageCheckAndSet = {
          if message != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'message'"))
          }
          message = $0
        }
        if let text = try container.decodeIfPresent(Intent.Message.Text?.self, forKey: .text) {
          try messageCheckAndSet(.text(text))
        }
        if let image = try container.decodeIfPresent(Intent.Message.Image?.self, forKey: .image) {
          try messageCheckAndSet(.image(image))
        }
        if let quickReplies = try container.decodeIfPresent(
          Intent.Message.QuickReplies?.self, forKey: .quickReplies)
        {
          try messageCheckAndSet(.quickReplies(quickReplies))
        }
        if let card = try container.decodeIfPresent(Intent.Message.Card?.self, forKey: .card) {
          try messageCheckAndSet(.card(card))
        }
        if let payload = try container.decodeIfPresent(
          GoogleCloudWkt.Struct?.self, forKey: .payload)
        {
          try messageCheckAndSet(.payload(payload))
        }
        if let simpleResponses = try container.decodeIfPresent(
          Intent.Message.SimpleResponses?.self, forKey: .simpleResponses)
        {
          try messageCheckAndSet(.simpleResponses(simpleResponses))
        }
        if let basicCard = try container.decodeIfPresent(
          Intent.Message.BasicCard?.self, forKey: .basicCard)
        {
          try messageCheckAndSet(.basicCard(basicCard))
        }
        if let suggestions = try container.decodeIfPresent(
          Intent.Message.Suggestions?.self, forKey: .suggestions)
        {
          try messageCheckAndSet(.suggestions(suggestions))
        }
        if let linkOutSuggestion = try container.decodeIfPresent(
          Intent.Message.LinkOutSuggestion?.self, forKey: .linkOutSuggestion)
        {
          try messageCheckAndSet(.linkOutSuggestion(linkOutSuggestion))
        }
        if let listSelect = try container.decodeIfPresent(
          Intent.Message.ListSelect?.self, forKey: .listSelect)
        {
          try messageCheckAndSet(.listSelect(listSelect))
        }
        if let carouselSelect = try container.decodeIfPresent(
          Intent.Message.CarouselSelect?.self, forKey: .carouselSelect)
        {
          try messageCheckAndSet(.carouselSelect(carouselSelect))
        }
        if let browseCarouselCard = try container.decodeIfPresent(
          Intent.Message.BrowseCarouselCard?.self, forKey: .browseCarouselCard)
        {
          try messageCheckAndSet(.browseCarouselCard(browseCarouselCard))
        }
        if let tableCard = try container.decodeIfPresent(
          Intent.Message.TableCard?.self, forKey: .tableCard)
        {
          try messageCheckAndSet(.tableCard(tableCard))
        }
        if let mediaContent = try container.decodeIfPresent(
          Intent.Message.MediaContent?.self, forKey: .mediaContent)
        {
          try messageCheckAndSet(.mediaContent(mediaContent))
        }
        self.message = message
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.platform, forKey: .platform)

        if let choice = self.message {
          switch choice {
          case .text(let value):
            try container.encode(value, forKey: .text)
          case .image(let value):
            try container.encode(value, forKey: .image)
          case .quickReplies(let value):
            try container.encode(value, forKey: .quickReplies)
          case .card(let value):
            try container.encode(value, forKey: .card)
          case .payload(let value):
            try container.encode(value, forKey: .payload)
          case .simpleResponses(let value):
            try container.encode(value, forKey: .simpleResponses)
          case .basicCard(let value):
            try container.encode(value, forKey: .basicCard)
          case .suggestions(let value):
            try container.encode(value, forKey: .suggestions)
          case .linkOutSuggestion(let value):
            try container.encode(value, forKey: .linkOutSuggestion)
          case .listSelect(let value):
            try container.encode(value, forKey: .listSelect)
          case .carouselSelect(let value):
            try container.encode(value, forKey: .carouselSelect)
          case .browseCarouselCard(let value):
            try container.encode(value, forKey: .browseCarouselCard)
          case .tableCard(let value):
            try container.encode(value, forKey: .tableCard)
          case .mediaContent(let value):
            try container.encode(value, forKey: .mediaContent)
          }
        }
      }

      /// The text response message.
      public struct Text: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. The collection of the agent's responses.
        public var text: [Swift.String] = []

        /// Initialize a new instance of `Text`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Text().with { $0.text = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.Text"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The image response message.
      public struct Image: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. The public URI to an image file.
        public var imageUri: Swift.String = Swift.String()

        /// Optional. A text description of the image to be used for accessibility,
        /// e.g., screen readers.
        public var accessibilityText: Swift.String = Swift.String()

        /// Initialize a new instance of `Image`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Image().with { $0.imageUri = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.Image"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The quick replies response message.
      public struct QuickReplies: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. The title of the collection of quick replies.
        public var title: Swift.String = Swift.String()

        /// Optional. The collection of quick replies.
        public var quickReplies: [Swift.String] = []

        /// Initialize a new instance of `QuickReplies`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = QuickReplies().with { $0.title = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.QuickReplies"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The card response message.
      public struct Card: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. The title of the card.
        public var title: Swift.String = Swift.String()

        /// Optional. The subtitle of the card.
        public var subtitle: Swift.String = Swift.String()

        /// Optional. The public URI to an image file for the card.
        public var imageUri: Swift.String = Swift.String()

        /// Optional. The collection of card buttons.
        public var buttons: [Intent.Message.Card.Button] = []

        /// Initialize a new instance of `Card`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Card().with { $0.title = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Contains information about a button.
        public struct Button: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Optional. The text to show on the button.
          public var text: Swift.String = Swift.String()

          /// Optional. The text to send back to the Dialogflow API or a URI to
          /// open.
          public var postback: Swift.String = Swift.String()

          /// Initialize a new instance of `Button`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = Button().with { $0.text = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.Card.Button"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.Card"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The simple response message containing speech or text.
      public struct SimpleResponse: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// One of text_to_speech or ssml must be provided. The plain text of the
        /// speech output. Mutually exclusive with ssml.
        public var textToSpeech: Swift.String = Swift.String()

        /// One of text_to_speech or ssml must be provided. Structured spoken
        /// response to the user in the SSML format. Mutually exclusive with
        /// text_to_speech.
        public var ssml: Swift.String = Swift.String()

        /// Optional. The text to display.
        public var displayText: Swift.String = Swift.String()

        /// Initialize a new instance of `SimpleResponse`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = SimpleResponse().with { $0.textToSpeech = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.SimpleResponse"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The collection of simple response candidates.
      /// This message in `QueryResult.fulfillment_messages` and
      /// `WebhookResponse.fulfillment_messages` should contain only one
      /// `SimpleResponse`.
      public struct SimpleResponses: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. The list of simple responses.
        public var simpleResponses: [Intent.Message.SimpleResponse] = []

        /// Initialize a new instance of `SimpleResponses`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = SimpleResponses().with { $0.simpleResponses = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.SimpleResponses"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The basic card message. Useful for displaying information.
      public struct BasicCard: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. The title of the card.
        public var title: Swift.String = Swift.String()

        /// Optional. The subtitle of the card.
        public var subtitle: Swift.String = Swift.String()

        /// Required, unless image is present. The body text of the card.
        public var formattedText: Swift.String = Swift.String()

        /// Optional. The image for the card.
        public var image: Intent.Message.Image? = nil

        /// Optional. The collection of card buttons.
        public var buttons: [Intent.Message.BasicCard.Button] = []

        /// Initialize a new instance of `BasicCard`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = BasicCard().with { $0.title = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// The button object that appears at the bottom of a card.
        public struct Button: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Required. The title of the button.
          public var title: Swift.String = Swift.String()

          /// Required. Action to take when a user taps on the button.
          public var openUriAction: Intent.Message.BasicCard.Button.OpenUriAction? = nil

          /// Initialize a new instance of `Button`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = Button().with { $0.title = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          /// Opens the given URI.
          public struct OpenUriAction: Codable, Equatable, GoogleCloudWkt._AnyPackable,
            Sendable
          {
            /// Required. The HTTP or HTTPS scheme URI.
            public var uri: Swift.String = Swift.String()

            /// Initialize a new instance of `OpenUriAction`.
            public init() {}

            /// Use `config` to return a new instance of this object, with some fields updated.
            ///
            /// Commonly used to initialize the value, for example:
            ///
            /// ```
            /// let value = OpenUriAction().with { $0.uri = ... }
            /// ```
            public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
              var copy = self
              try config(&copy)
              return copy
            }

            public static var _anyTypeUrl: Swift.String {
              return
                "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.BasicCard.Button.OpenUriAction"
            }
            public init(fromAny any: GoogleCloudWkt.`Any`) throws {
              self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
            }
            public func _pack() throws -> GoogleCloudWkt.Struct {
              return try GoogleCloudWkt._slowAnySerialize(message: self)
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.BasicCard.Button"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.BasicCard"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The suggestion chip message that the user can tap to quickly post a reply
      /// to the conversation.
      public struct Suggestion: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. The text shown the in the suggestion chip.
        public var title: Swift.String = Swift.String()

        /// Initialize a new instance of `Suggestion`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Suggestion().with { $0.title = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.Suggestion"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The collection of suggestions.
      public struct Suggestions: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. The list of suggested replies.
        public var suggestions: [Intent.Message.Suggestion] = []

        /// Initialize a new instance of `Suggestions`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = Suggestions().with { $0.suggestions = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.Suggestions"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The suggestion chip message that allows the user to jump out to the app
      /// or website associated with this agent.
      public struct LinkOutSuggestion: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. The name of the app or site this chip is linking to.
        public var destinationName: Swift.String = Swift.String()

        /// Required. The URI of the app or site to open when the user taps the
        /// suggestion chip.
        public var uri: Swift.String = Swift.String()

        /// Initialize a new instance of `LinkOutSuggestion`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = LinkOutSuggestion().with { $0.destinationName = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.LinkOutSuggestion"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The card for presenting a list of options to select from.
      public struct ListSelect: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. The overall title of the list.
        public var title: Swift.String = Swift.String()

        /// Required. List items.
        public var items: [Intent.Message.ListSelect.Item] = []

        /// Optional. Subtitle of the list.
        public var subtitle: Swift.String = Swift.String()

        /// Initialize a new instance of `ListSelect`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ListSelect().with { $0.title = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// An item in the list.
        public struct Item: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Required. Additional information about this option.
          public var info: Intent.Message.SelectItemInfo? = nil

          /// Required. The title of the list item.
          public var title: Swift.String = Swift.String()

          /// Optional. The main text describing the item.
          public var description: Swift.String = Swift.String()

          /// Optional. The image to display.
          public var image: Intent.Message.Image? = nil

          /// Initialize a new instance of `Item`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = Item().with { $0.info = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.ListSelect.Item"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.ListSelect"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The card for presenting a carousel of options to select from.
      public struct CarouselSelect: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. Carousel items.
        public var items: [Intent.Message.CarouselSelect.Item] = []

        /// Initialize a new instance of `CarouselSelect`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = CarouselSelect().with { $0.items = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// An item in the carousel.
        public struct Item: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Required. Additional info about the option item.
          public var info: Intent.Message.SelectItemInfo? = nil

          /// Required. Title of the carousel item.
          public var title: Swift.String = Swift.String()

          /// Optional. The body text of the card.
          public var description: Swift.String = Swift.String()

          /// Optional. The image to display.
          public var image: Intent.Message.Image? = nil

          /// Initialize a new instance of `Item`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = Item().with { $0.info = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.CarouselSelect.Item"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.CarouselSelect"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Additional info about the select item for when it is triggered in a
      /// dialog.
      public struct SelectItemInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. A unique key that will be sent back to the agent if this
        /// response is given.
        public var key: Swift.String = Swift.String()

        /// Optional. A list of synonyms that can also be used to trigger this
        /// item in dialog.
        public var synonyms: [Swift.String] = []

        /// Initialize a new instance of `SelectItemInfo`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = SelectItemInfo().with { $0.key = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.SelectItemInfo"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The media content card for Actions on Google.
      public struct MediaContent: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. What type of media is the content (ie "audio").
        public var mediaType: Intent.Message.MediaContent.ResponseMediaType = Intent.Message
          .MediaContent.ResponseMediaType()

        /// Required. List of media objects.
        public var mediaObjects: [Intent.Message.MediaContent.ResponseMediaObject] = []

        /// Initialize a new instance of `MediaContent`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = MediaContent().with { $0.mediaType = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Response media object for media content card.
        public struct ResponseMediaObject: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Required. Name of media card.
          public var name: Swift.String = Swift.String()

          /// Optional. Description of media card.
          public var description: Swift.String = Swift.String()

          /// Required. Url where the media is stored.
          public var contentUrl: Swift.String = Swift.String()

          /// Image to show with the media card.
          public var image: OneOf_Image? = nil

          /// Initialize a new instance of `ResponseMediaObject`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = ResponseMediaObject().with { $0.name = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          private enum CodingKeys: Swift.String, CodingKey {
            case name = "name"
            case description = "description"
            case largeImage = "largeImage"
            case icon = "icon"
            case contentUrl = "contentUrl"
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decode(Swift.String.self, forKey: .name)
            self.description = try container.decode(Swift.String.self, forKey: .description)
            self.contentUrl = try container.decode(Swift.String.self, forKey: .contentUrl)

            var image: OneOf_Image? = nil
            let imageCheckAndSet = {
              if image != nil {
                throw DecodingError.dataCorrupted(
                  DecodingError.Context(
                    codingPath: decoder.codingPath,
                    debugDescription: "Multiple values set for oneof 'image'"))
              }
              image = $0
            }
            if let largeImage = try container.decodeIfPresent(
              Intent.Message.Image?.self, forKey: .largeImage)
            {
              try imageCheckAndSet(.largeImage(largeImage))
            }
            if let icon = try container.decodeIfPresent(Intent.Message.Image?.self, forKey: .icon) {
              try imageCheckAndSet(.icon(icon))
            }
            self.image = image
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.name, forKey: .name)
            try container.encode(self.description, forKey: .description)
            try container.encode(self.contentUrl, forKey: .contentUrl)

            if let choice = self.image {
              switch choice {
              case .largeImage(let value):
                try container.encode(value, forKey: .largeImage)
              case .icon(let value):
                try container.encode(value, forKey: .icon)
              }
            }
          }

          /// Image to show with the media card.
          public enum OneOf_Image: Codable, Equatable, Sendable {
            /// Optional. Image to display above media content.
            indirect case largeImage(Intent.Message.Image?)
            /// Optional. Icon to display above media content.
            indirect case icon(Intent.Message.Image?)
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.MediaContent.ResponseMediaObject"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Format of response media type.
        public enum ResponseMediaType: Codable, Equatable, Sendable {
          /// Unspecified.
          case unspecified
          /// Response media type is audio.
          case audio
          /// Encodes an unknown integer value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownIntValue(Int)
          /// Encodes an unknown string value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownStringValue(String)

          public init() {
            self = .unspecified
          }

          /// Returns the integer value associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown string value, this returns `nil`.
          public var intValue: Int? {
            switch self {
            case .unspecified: return 0
            case .audio: return 1
            case .unknownIntValue(let v): return v
            case .unknownStringValue: return nil
            }
          }

          /// Returns the string value (or name) associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
          public var stringValue: Swift.String? {
            switch self {
            case .unspecified: return "RESPONSE_MEDIA_TYPE_UNSPECIFIED"
            case .audio: return "AUDIO"
            case .unknownIntValue: return nil
            case .unknownStringValue(let v): return v
            }
          }

          /// Initialize from a string value.
          ///
          /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ResponseMediaType/unknownStringValue(_:)).
          public init(stringValue: Swift.String) {
            switch stringValue {
            case "RESPONSE_MEDIA_TYPE_UNSPECIFIED": self = .unspecified
            case "AUDIO": self = .audio
            default: self = .unknownStringValue(stringValue)
            }
          }

          /// Initialize from an integer value.
          ///
          /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ResponseMediaType/unknownIntValue(_:)).
          public init(intValue: Int) {
            switch intValue {
            case 0: self = .unspecified
            case 1: self = .audio
            default: self = .unknownIntValue(intValue)
            }
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            if let v = try? container.decode(Int.self) {
              self.init(intValue: v)
              return
            }
            if let s = try? container.decode(String.self) {
              if let v = Int(s) {
                self.init(intValue: v)
              } else {
                self.init(stringValue: s)
              }
              return
            }
            throw DecodingError.dataCorruptedError(
              in: container, debugDescription: "Expected enum value, must be integer or string.")
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            switch self {
            case .unspecified: return try container.encode(0)
            case .audio: return try container.encode(1)
            case .unknownIntValue(let v): return try container.encode(v)
            case .unknownStringValue(let v): return try container.encode(v)
            }
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.MediaContent"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Browse Carousel Card for Actions on Google.
      /// https://developers.google.com/actions/assistant/responses#browsing_carousel
      public struct BrowseCarouselCard: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. List of items in the Browse Carousel Card. Minimum of two
        /// items, maximum of ten.
        public var items: [Intent.Message.BrowseCarouselCard.BrowseCarouselCardItem] = []

        /// Optional. Settings for displaying the image. Applies to every image in
        /// [items][google.cloud.dialogflow.v2.Intent.Message.BrowseCarouselCard.items].
        ///
        /// [google.cloud.dialogflow.v2.Intent.Message.BrowseCarouselCard.items]: <doc:Intent/Message/BrowseCarouselCard/items>
        public var imageDisplayOptions: Intent.Message.BrowseCarouselCard.ImageDisplayOptions =
          Intent.Message.BrowseCarouselCard.ImageDisplayOptions()

        /// Initialize a new instance of `BrowseCarouselCard`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = BrowseCarouselCard().with { $0.items = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Browsing carousel tile
        public struct BrowseCarouselCardItem: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Required. Action to present to the user.
          public var openUriAction:
            Intent.Message.BrowseCarouselCard.BrowseCarouselCardItem.OpenUrlAction? = nil

          /// Required. Title of the carousel item. Maximum of two lines of text.
          public var title: Swift.String = Swift.String()

          /// Optional. Description of the carousel item. Maximum of four lines of
          /// text.
          public var description: Swift.String = Swift.String()

          /// Optional. Hero image for the carousel item.
          public var image: Intent.Message.Image? = nil

          /// Optional. Text that appears at the bottom of the Browse Carousel
          /// Card. Maximum of one line of text.
          public var footer: Swift.String = Swift.String()

          /// Initialize a new instance of `BrowseCarouselCardItem`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = BrowseCarouselCardItem().with { $0.openUriAction = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          /// Actions on Google action to open a given url.
          public struct OpenUrlAction: Codable, Equatable, GoogleCloudWkt._AnyPackable,
            Sendable
          {
            /// Required. URL
            public var url: Swift.String = Swift.String()

            /// Optional. Specifies the type of viewer that is used when opening
            /// the URL. Defaults to opening via web browser.
            public var urlTypeHint:
              Intent.Message.BrowseCarouselCard.BrowseCarouselCardItem.OpenUrlAction.UrlTypeHint =
                Intent.Message.BrowseCarouselCard.BrowseCarouselCardItem.OpenUrlAction.UrlTypeHint()

            /// Initialize a new instance of `OpenUrlAction`.
            public init() {}

            /// Use `config` to return a new instance of this object, with some fields updated.
            ///
            /// Commonly used to initialize the value, for example:
            ///
            /// ```
            /// let value = OpenUrlAction().with { $0.url = ... }
            /// ```
            public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
              var copy = self
              try config(&copy)
              return copy
            }

            /// Type of the URI.
            public enum UrlTypeHint: Codable, Equatable, Sendable {
              /// Unspecified
              case unspecified
              /// Url would be an amp action
              case ampAction
              /// URL that points directly to AMP content, or to a canonical URL
              /// which refers to AMP content via <link rel="amphtml">.
              case ampContent
              /// Encodes an unknown integer value.
              ///
              /// The most common cause for an unknown values is for the service to send
              /// a value unknown to the library. We recommend you update your library to
              /// the latest version.
              case unknownIntValue(Int)
              /// Encodes an unknown string value.
              ///
              /// The most common cause for an unknown values is for the service to send
              /// a value unknown to the library. We recommend you update your library to
              /// the latest version.
              case unknownStringValue(String)

              public init() {
                self = .unspecified
              }

              /// Returns the integer value associated with the enumeration.
              ///
              /// If the enumeration was initialized with an unknown string value, this returns `nil`.
              public var intValue: Int? {
                switch self {
                case .unspecified: return 0
                case .ampAction: return 1
                case .ampContent: return 2
                case .unknownIntValue(let v): return v
                case .unknownStringValue: return nil
                }
              }

              /// Returns the string value (or name) associated with the enumeration.
              ///
              /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
              public var stringValue: Swift.String? {
                switch self {
                case .unspecified: return "URL_TYPE_HINT_UNSPECIFIED"
                case .ampAction: return "AMP_ACTION"
                case .ampContent: return "AMP_CONTENT"
                case .unknownIntValue: return nil
                case .unknownStringValue(let v): return v
                }
              }

              /// Initialize from a string value.
              ///
              /// If the value is unknown, this initializes to [`unknownStringValue`](doc:UrlTypeHint/unknownStringValue(_:)).
              public init(stringValue: Swift.String) {
                switch stringValue {
                case "URL_TYPE_HINT_UNSPECIFIED": self = .unspecified
                case "AMP_ACTION": self = .ampAction
                case "AMP_CONTENT": self = .ampContent
                default: self = .unknownStringValue(stringValue)
                }
              }

              /// Initialize from an integer value.
              ///
              /// If the value is unknown, this initializes to [`unknownIntValue`](doc:UrlTypeHint/unknownIntValue(_:)).
              public init(intValue: Int) {
                switch intValue {
                case 0: self = .unspecified
                case 1: self = .ampAction
                case 2: self = .ampContent
                default: self = .unknownIntValue(intValue)
                }
              }

              public init(from decoder: Decoder) throws {
                let container = try decoder.singleValueContainer()
                if let v = try? container.decode(Int.self) {
                  self.init(intValue: v)
                  return
                }
                if let s = try? container.decode(String.self) {
                  if let v = Int(s) {
                    self.init(intValue: v)
                  } else {
                    self.init(stringValue: s)
                  }
                  return
                }
                throw DecodingError.dataCorruptedError(
                  in: container, debugDescription: "Expected enum value, must be integer or string."
                )
              }

              public func encode(to encoder: Encoder) throws {
                var container = encoder.singleValueContainer()
                switch self {
                case .unspecified: return try container.encode(0)
                case .ampAction: return try container.encode(1)
                case .ampContent: return try container.encode(2)
                case .unknownIntValue(let v): return try container.encode(v)
                case .unknownStringValue(let v): return try container.encode(v)
                }
              }
            }

            public static var _anyTypeUrl: Swift.String {
              return
                "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.BrowseCarouselCard.BrowseCarouselCardItem.OpenUrlAction"
            }
            public init(fromAny any: GoogleCloudWkt.`Any`) throws {
              self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
            }
            public func _pack() throws -> GoogleCloudWkt.Struct {
              return try GoogleCloudWkt._slowAnySerialize(message: self)
            }
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.BrowseCarouselCard.BrowseCarouselCardItem"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Image display options for Actions on Google. This should be used for
        /// when the image's aspect ratio does not match the image container's
        /// aspect ratio.
        public enum ImageDisplayOptions: Codable, Equatable, Sendable {
          /// Fill the gaps between the image and the image container with gray
          /// bars.
          case unspecified
          /// Fill the gaps between the image and the image container with gray
          /// bars.
          case gray
          /// Fill the gaps between the image and the image container with white
          /// bars.
          case white
          /// Image is scaled such that the image width and height match or exceed
          /// the container dimensions. This may crop the top and bottom of the
          /// image if the scaled image height is greater than the container
          /// height, or crop the left and right of the image if the scaled image
          /// width is greater than the container width. This is similar to "Zoom
          /// Mode" on a widescreen TV when playing a 4:3 video.
          case cropped
          /// Pad the gaps between image and image frame with a blurred copy of the
          /// same image.
          case blurredBackground
          /// Encodes an unknown integer value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownIntValue(Int)
          /// Encodes an unknown string value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownStringValue(String)

          public init() {
            self = .unspecified
          }

          /// Returns the integer value associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown string value, this returns `nil`.
          public var intValue: Int? {
            switch self {
            case .unspecified: return 0
            case .gray: return 1
            case .white: return 2
            case .cropped: return 3
            case .blurredBackground: return 4
            case .unknownIntValue(let v): return v
            case .unknownStringValue: return nil
            }
          }

          /// Returns the string value (or name) associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
          public var stringValue: Swift.String? {
            switch self {
            case .unspecified: return "IMAGE_DISPLAY_OPTIONS_UNSPECIFIED"
            case .gray: return "GRAY"
            case .white: return "WHITE"
            case .cropped: return "CROPPED"
            case .blurredBackground: return "BLURRED_BACKGROUND"
            case .unknownIntValue: return nil
            case .unknownStringValue(let v): return v
            }
          }

          /// Initialize from a string value.
          ///
          /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ImageDisplayOptions/unknownStringValue(_:)).
          public init(stringValue: Swift.String) {
            switch stringValue {
            case "IMAGE_DISPLAY_OPTIONS_UNSPECIFIED": self = .unspecified
            case "GRAY": self = .gray
            case "WHITE": self = .white
            case "CROPPED": self = .cropped
            case "BLURRED_BACKGROUND": self = .blurredBackground
            default: self = .unknownStringValue(stringValue)
            }
          }

          /// Initialize from an integer value.
          ///
          /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ImageDisplayOptions/unknownIntValue(_:)).
          public init(intValue: Int) {
            switch intValue {
            case 0: self = .unspecified
            case 1: self = .gray
            case 2: self = .white
            case 3: self = .cropped
            case 4: self = .blurredBackground
            default: self = .unknownIntValue(intValue)
            }
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            if let v = try? container.decode(Int.self) {
              self.init(intValue: v)
              return
            }
            if let s = try? container.decode(String.self) {
              if let v = Int(s) {
                self.init(intValue: v)
              } else {
                self.init(stringValue: s)
              }
              return
            }
            throw DecodingError.dataCorruptedError(
              in: container, debugDescription: "Expected enum value, must be integer or string.")
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            switch self {
            case .unspecified: return try container.encode(0)
            case .gray: return try container.encode(1)
            case .white: return try container.encode(2)
            case .cropped: return try container.encode(3)
            case .blurredBackground: return try container.encode(4)
            case .unknownIntValue(let v): return try container.encode(v)
            case .unknownStringValue(let v): return try container.encode(v)
            }
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.BrowseCarouselCard"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Table card for Actions on Google.
      public struct TableCard: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. Title of the card.
        public var title: Swift.String = Swift.String()

        /// Optional. Subtitle to the title.
        public var subtitle: Swift.String = Swift.String()

        /// Optional. Image which should be displayed on the card.
        public var image: Intent.Message.Image? = nil

        /// Optional. Display properties for the columns in this table.
        public var columnProperties: [Intent.Message.ColumnProperties] = []

        /// Optional. Rows in this table of data.
        public var rows: [Intent.Message.TableCardRow] = []

        /// Optional. List of buttons for the card.
        public var buttons: [Intent.Message.BasicCard.Button] = []

        /// Initialize a new instance of `TableCard`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = TableCard().with { $0.title = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.TableCard"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Column properties for
      /// [TableCard][google.cloud.dialogflow.v2.Intent.Message.TableCard].
      ///
      /// [google.cloud.dialogflow.v2.Intent.Message.TableCard]: <doc:Intent/Message/TableCard>
      public struct ColumnProperties: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. Column heading.
        public var header: Swift.String = Swift.String()

        /// Optional. Defines text alignment for all cells in this column.
        public var horizontalAlignment: Intent.Message.ColumnProperties.HorizontalAlignment = Intent
          .Message.ColumnProperties.HorizontalAlignment()

        /// Initialize a new instance of `ColumnProperties`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = ColumnProperties().with { $0.header = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Text alignments within a cell.
        public enum HorizontalAlignment: Codable, Equatable, Sendable {
          /// Text is aligned to the leading edge of the column.
          case unspecified
          /// Text is aligned to the leading edge of the column.
          case leading
          /// Text is centered in the column.
          case center
          /// Text is aligned to the trailing edge of the column.
          case trailing
          /// Encodes an unknown integer value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownIntValue(Int)
          /// Encodes an unknown string value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownStringValue(String)

          public init() {
            self = .unspecified
          }

          /// Returns the integer value associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown string value, this returns `nil`.
          public var intValue: Int? {
            switch self {
            case .unspecified: return 0
            case .leading: return 1
            case .center: return 2
            case .trailing: return 3
            case .unknownIntValue(let v): return v
            case .unknownStringValue: return nil
            }
          }

          /// Returns the string value (or name) associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
          public var stringValue: Swift.String? {
            switch self {
            case .unspecified: return "HORIZONTAL_ALIGNMENT_UNSPECIFIED"
            case .leading: return "LEADING"
            case .center: return "CENTER"
            case .trailing: return "TRAILING"
            case .unknownIntValue: return nil
            case .unknownStringValue(let v): return v
            }
          }

          /// Initialize from a string value.
          ///
          /// If the value is unknown, this initializes to [`unknownStringValue`](doc:HorizontalAlignment/unknownStringValue(_:)).
          public init(stringValue: Swift.String) {
            switch stringValue {
            case "HORIZONTAL_ALIGNMENT_UNSPECIFIED": self = .unspecified
            case "LEADING": self = .leading
            case "CENTER": self = .center
            case "TRAILING": self = .trailing
            default: self = .unknownStringValue(stringValue)
            }
          }

          /// Initialize from an integer value.
          ///
          /// If the value is unknown, this initializes to [`unknownIntValue`](doc:HorizontalAlignment/unknownIntValue(_:)).
          public init(intValue: Int) {
            switch intValue {
            case 0: self = .unspecified
            case 1: self = .leading
            case 2: self = .center
            case 3: self = .trailing
            default: self = .unknownIntValue(intValue)
            }
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            if let v = try? container.decode(Int.self) {
              self.init(intValue: v)
              return
            }
            if let s = try? container.decode(String.self) {
              if let v = Int(s) {
                self.init(intValue: v)
              } else {
                self.init(stringValue: s)
              }
              return
            }
            throw DecodingError.dataCorruptedError(
              in: container, debugDescription: "Expected enum value, must be integer or string.")
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            switch self {
            case .unspecified: return try container.encode(0)
            case .leading: return try container.encode(1)
            case .center: return try container.encode(2)
            case .trailing: return try container.encode(3)
            case .unknownIntValue(let v): return try container.encode(v)
            case .unknownStringValue(let v): return try container.encode(v)
            }
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.ColumnProperties"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Row of [TableCard][google.cloud.dialogflow.v2.Intent.Message.TableCard].
      ///
      /// [google.cloud.dialogflow.v2.Intent.Message.TableCard]: <doc:Intent/Message/TableCard>
      public struct TableCardRow: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. List of cells that make up this row.
        public var cells: [Intent.Message.TableCardCell] = []

        /// Optional. Whether to add a visual divider after this row.
        public var dividerAfter: Swift.Bool = Swift.Bool()

        /// Initialize a new instance of `TableCardRow`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = TableCardRow().with { $0.cells = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.TableCardRow"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// Cell of
      /// [TableCardRow][google.cloud.dialogflow.v2.Intent.Message.TableCardRow].
      ///
      /// [google.cloud.dialogflow.v2.Intent.Message.TableCardRow]: <doc:Intent/Message/TableCardRow>
      public struct TableCardCell: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. Text in this cell.
        public var text: Swift.String = Swift.String()

        /// Initialize a new instance of `TableCardCell`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = TableCardCell().with { $0.text = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message.TableCardCell"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      /// The rich response message integration platform. See
      /// [Integrations](https://cloud.google.com/dialogflow/docs/integrations).
      public enum Platform: Codable, Equatable, Sendable {
        /// Default platform.
        case unspecified
        /// Facebook.
        case facebook
        /// Slack.
        case slack
        /// Telegram.
        case telegram
        /// Kik.
        case kik
        /// Skype.
        case skype
        /// Line.
        case line
        /// Viber.
        case viber
        /// Google Assistant
        /// See [Dialogflow webhook
        /// format](https://developers.google.com/assistant/actions/build/json/dialogflow-webhook-json)
        case actionsOnGoogle
        /// Google Hangouts.
        case googleHangouts
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .facebook: return 1
          case .slack: return 2
          case .telegram: return 3
          case .kik: return 4
          case .skype: return 5
          case .line: return 6
          case .viber: return 7
          case .actionsOnGoogle: return 8
          case .googleHangouts: return 11
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: Swift.String? {
          switch self {
          case .unspecified: return "PLATFORM_UNSPECIFIED"
          case .facebook: return "FACEBOOK"
          case .slack: return "SLACK"
          case .telegram: return "TELEGRAM"
          case .kik: return "KIK"
          case .skype: return "SKYPE"
          case .line: return "LINE"
          case .viber: return "VIBER"
          case .actionsOnGoogle: return "ACTIONS_ON_GOOGLE"
          case .googleHangouts: return "GOOGLE_HANGOUTS"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Platform/unknownStringValue(_:)).
        public init(stringValue: Swift.String) {
          switch stringValue {
          case "PLATFORM_UNSPECIFIED": self = .unspecified
          case "FACEBOOK": self = .facebook
          case "SLACK": self = .slack
          case "TELEGRAM": self = .telegram
          case "KIK": self = .kik
          case "SKYPE": self = .skype
          case "LINE": self = .line
          case "VIBER": self = .viber
          case "ACTIONS_ON_GOOGLE": self = .actionsOnGoogle
          case "GOOGLE_HANGOUTS": self = .googleHangouts
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Platform/unknownIntValue(_:)).
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .facebook
          case 2: self = .slack
          case 3: self = .telegram
          case 4: self = .kik
          case 5: self = .skype
          case 6: self = .line
          case 7: self = .viber
          case 8: self = .actionsOnGoogle
          case 11: self = .googleHangouts
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode(0)
          case .facebook: return try container.encode(1)
          case .slack: return try container.encode(2)
          case .telegram: return try container.encode(3)
          case .kik: return try container.encode(4)
          case .skype: return try container.encode(5)
          case .line: return try container.encode(6)
          case .viber: return try container.encode(7)
          case .actionsOnGoogle: return try container.encode(8)
          case .googleHangouts: return try container.encode(11)
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      /// Required. The rich response message.
      public enum OneOf_Message: Codable, Equatable, Sendable {
        /// The text response.
        indirect case text(Intent.Message.Text?)
        /// The image response.
        indirect case image(Intent.Message.Image?)
        /// The quick replies response.
        indirect case quickReplies(Intent.Message.QuickReplies?)
        /// The card response.
        indirect case card(Intent.Message.Card?)
        /// A custom platform-specific response.
        indirect case payload(GoogleCloudWkt.Struct?)
        /// The voice and text-only responses for Actions on Google.
        indirect case simpleResponses(Intent.Message.SimpleResponses?)
        /// The basic card response for Actions on Google.
        indirect case basicCard(Intent.Message.BasicCard?)
        /// The suggestion chips for Actions on Google.
        indirect case suggestions(Intent.Message.Suggestions?)
        /// The link out suggestion chip for Actions on Google.
        indirect case linkOutSuggestion(Intent.Message.LinkOutSuggestion?)
        /// The list card response for Actions on Google.
        indirect case listSelect(Intent.Message.ListSelect?)
        /// The carousel card response for Actions on Google.
        indirect case carouselSelect(Intent.Message.CarouselSelect?)
        /// Browse carousel card for Actions on Google.
        indirect case browseCarouselCard(Intent.Message.BrowseCarouselCard?)
        /// Table card for Actions on Google.
        indirect case tableCard(Intent.Message.TableCard?)
        /// The media content card for Actions on Google.
        indirect case mediaContent(Intent.Message.MediaContent?)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.Message"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents a single followup intent in the chain.
    public struct FollowupIntentInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// The unique identifier of the followup intent.
      /// Format: `projects/<Project ID>/agent/intents/<Intent ID>`.
      public var followupIntentName: Swift.String = Swift.String()

      /// The unique identifier of the followup intent's parent.
      /// Format: `projects/<Project ID>/agent/intents/<Intent ID>`.
      public var parentFollowupIntentName: Swift.String = Swift.String()

      /// Initialize a new instance of `FollowupIntentInfo`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = FollowupIntentInfo().with { $0.followupIntentName = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.v2.Intent.FollowupIntentInfo"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents the different states that webhooks can be in.
    public enum WebhookState: Codable, Equatable, Sendable {
      /// Webhook is disabled in the agent and in the intent.
      case unspecified
      /// Webhook is enabled in the agent and in the intent.
      case enabled
      /// Webhook is enabled in the agent and in the intent. Also, each slot
      /// filling prompt is forwarded to the webhook.
      case enabledForSlotFilling
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .enabled: return 1
        case .enabledForSlotFilling: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "WEBHOOK_STATE_UNSPECIFIED"
        case .enabled: return "WEBHOOK_STATE_ENABLED"
        case .enabledForSlotFilling: return "WEBHOOK_STATE_ENABLED_FOR_SLOT_FILLING"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:WebhookState/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "WEBHOOK_STATE_UNSPECIFIED": self = .unspecified
        case "WEBHOOK_STATE_ENABLED": self = .enabled
        case "WEBHOOK_STATE_ENABLED_FOR_SLOT_FILLING": self = .enabledForSlotFilling
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:WebhookState/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .enabled
        case 2: self = .enabledForSlotFilling
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .enabled: return try container.encode(1)
        case .enabledForSlotFilling: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.v2.Intent"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
