// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if SipTrunks
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWKT

  /// Represents a connection for SIP Trunk.
  public struct Connection: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Output only. The unique identifier of the SIP Trunk connection.
    public var connectionId: Swift.String = Swift.String()

    /// Output only. State of the connection.
    public var state: Connection.State = Connection.State()

    /// Output only. When the connection status changed.
    public var updateTime: GoogleCloudWKT.Timestamp? = nil

    /// Output only. The error details for the connection. Only populated when
    /// authentication errors occur.
    public var errorDetails: Connection.ErrorDetails? = nil

    /// Initialize a new instance of `Connection`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Connection().with { $0.connectionId = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// The error details of Sip Trunk connection authentication.
    public struct ErrorDetails: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Output only. The status of the certificate authentication.
      public var certificateState: Connection.CertificateState? = nil

      /// The error message provided from SIP trunking auth service
      public var errorMessage: Swift.String? = nil

      /// Initialize a new instance of `ErrorDetails`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ErrorDetails().with { $0.certificateState = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.dialogflow.v2.Connection.ErrorDetails"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// The state of Sip Trunk connection.
    public enum State: Codable, Equatable, Sendable {
      /// SIP Trunk connection state is Not specified.
      case unspecified
      /// SIP Trunk connection is connected.
      case connected
      /// SIP Trunk connection is disconnected.
      case disconnected
      /// SIP Trunk connection has authentication error.
      case authenticationFailed
      /// SIP Trunk connection is keepalive.
      case keepalive
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .connected: return 1
        case .disconnected: return 2
        case .authenticationFailed: return 3
        case .keepalive: return 4
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "STATE_UNSPECIFIED"
        case .connected: return "CONNECTED"
        case .disconnected: return "DISCONNECTED"
        case .authenticationFailed: return "AUTHENTICATION_FAILED"
        case .keepalive: return "KEEPALIVE"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "STATE_UNSPECIFIED": self = .unspecified
        case "CONNECTED": self = .connected
        case "DISCONNECTED": self = .disconnected
        case "AUTHENTICATION_FAILED": self = .authenticationFailed
        case "KEEPALIVE": self = .keepalive
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .connected
        case 2: self = .disconnected
        case 3: self = .authenticationFailed
        case 4: self = .keepalive
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .connected: return try container.encode(1)
        case .disconnected: return try container.encode(2)
        case .authenticationFailed: return try container.encode(3)
        case .keepalive: return try container.encode(4)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// The state of Sip Trunk certificate authentication.
    public enum CertificateState: Codable, Equatable, Sendable {
      /// Certificate state is not specified.
      case unspecified
      /// Certificate is valid.
      case certificateValid
      /// Catch all for any error not specified.
      case certificateInvalid
      /// Certificate leaf node has expired.
      case certificateExpired
      /// There is no hostname defined to authenticate in SipTrunkingServer.
      case certificateHostnameNotFound
      /// No path found from the leaf certificate to any root.
      case certificateUnauthenticated
      /// Trust store does not exist.
      case certificateTrustStoreNotFound
      /// Hostname has invalid format.
      case certificateHostnameInvalidFormat
      /// Certificate has exhausted its quota.
      case certificateQuotaExceeded
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .certificateValid: return 1
        case .certificateInvalid: return 2
        case .certificateExpired: return 3
        case .certificateHostnameNotFound: return 4
        case .certificateUnauthenticated: return 5
        case .certificateTrustStoreNotFound: return 6
        case .certificateHostnameInvalidFormat: return 7
        case .certificateQuotaExceeded: return 8
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "CERTIFICATE_STATE_UNSPECIFIED"
        case .certificateValid: return "CERTIFICATE_VALID"
        case .certificateInvalid: return "CERTIFICATE_INVALID"
        case .certificateExpired: return "CERTIFICATE_EXPIRED"
        case .certificateHostnameNotFound: return "CERTIFICATE_HOSTNAME_NOT_FOUND"
        case .certificateUnauthenticated: return "CERTIFICATE_UNAUTHENTICATED"
        case .certificateTrustStoreNotFound: return "CERTIFICATE_TRUST_STORE_NOT_FOUND"
        case .certificateHostnameInvalidFormat: return "CERTIFICATE_HOSTNAME_INVALID_FORMAT"
        case .certificateQuotaExceeded: return "CERTIFICATE_QUOTA_EXCEEDED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:CertificateState/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "CERTIFICATE_STATE_UNSPECIFIED": self = .unspecified
        case "CERTIFICATE_VALID": self = .certificateValid
        case "CERTIFICATE_INVALID": self = .certificateInvalid
        case "CERTIFICATE_EXPIRED": self = .certificateExpired
        case "CERTIFICATE_HOSTNAME_NOT_FOUND": self = .certificateHostnameNotFound
        case "CERTIFICATE_UNAUTHENTICATED": self = .certificateUnauthenticated
        case "CERTIFICATE_TRUST_STORE_NOT_FOUND": self = .certificateTrustStoreNotFound
        case "CERTIFICATE_HOSTNAME_INVALID_FORMAT": self = .certificateHostnameInvalidFormat
        case "CERTIFICATE_QUOTA_EXCEEDED": self = .certificateQuotaExceeded
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:CertificateState/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .certificateValid
        case 2: self = .certificateInvalid
        case 3: self = .certificateExpired
        case 4: self = .certificateHostnameNotFound
        case 5: self = .certificateUnauthenticated
        case 6: self = .certificateTrustStoreNotFound
        case 7: self = .certificateHostnameInvalidFormat
        case 8: self = .certificateQuotaExceeded
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .certificateValid: return try container.encode(1)
        case .certificateInvalid: return try container.encode(2)
        case .certificateExpired: return try container.encode(3)
        case .certificateHostnameNotFound: return try container.encode(4)
        case .certificateUnauthenticated: return try container.encode(5)
        case .certificateTrustStoreNotFound: return try container.encode(6)
        case .certificateHostnameInvalidFormat: return try container.encode(7)
        case .certificateQuotaExceeded: return try container.encode(8)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.dialogflow.v2.Connection"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }
#endif
